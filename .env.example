# MongoDB connection string (replace <username>, <password> and <cluster> with your values)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/forest_fire?retryWrites=true&w=majority

# Server port (optional, defaults to 3000)
PORT=3000
